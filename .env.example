# Server-only env. The app works WITHOUT any key (deterministic seed/template fallback).
# For live Claude analysis, copy to .env.local (gitignored) and fill in a real key.
ANTHROPIC_API_KEY=

# optional overrides (have safe code defaults)
# ANALYZE_MODEL=claude-opus-4-8
# EXPLAIN_MODEL=claude-opus-4-8
# ANALYZE_EFFORT=medium
# DESIGN_EFFORT=medium
