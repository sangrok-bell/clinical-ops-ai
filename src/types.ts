// Design enums — single source of truth for tones/variants used across the
// ui primitives and feature components. Register a new tone/variant here first.

export type PillTone = "positive" | "magenta" | "danger";
export type ToastVariant = "success" | "info" | "ghost";
export type TagTone = "danger" | "warning" | "caution" | "positive";
