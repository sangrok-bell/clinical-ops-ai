// NOTE: scaffold placeholder. Final form (brand header + <SetupShell/>) lands in
// the IA phase (new-features/19). Kept minimal so the app compiles + Tier-0
// app-shell check (export default) passes while the product is built bottom-up.
export default function App() {
  return (
    <div className="grid min-h-screen place-items-center bg-canvas text-ink">
      <p className="text-dim">Clinical Ops AI — scaffold OK</p>
    </div>
  );
}
